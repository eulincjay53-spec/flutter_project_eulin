import 'package:flutter/material.dart';

class notificatiion extends StatelessWidget {
  const notificatiion({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}